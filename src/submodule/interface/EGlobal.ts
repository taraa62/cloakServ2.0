export enum EItemDomainController {
    CONTROLLER = "CONTROLLER",
    ACTION = "ACTION",
    HEADER = "HEADER"
}

export enum EProcessEdit {
    PRE = "PRE",
    POST = "POST"
}

export enum EResourceFolder {
    "def" = "def",
    "html" = "__html",
    "sub" = "__subDomains"
}

export enum EDomainType {
    super = 'mainDomain',
    child = 'subDomain'
}
